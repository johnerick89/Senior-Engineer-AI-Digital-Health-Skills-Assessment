# Core
chainlit==2.5.5
sniffio

# OpenTelemetry - pinned to compatible versions
opentelemetry-api>=1.26.0,<2.0.0
opentelemetry-sdk>=1.26.0,<2.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.26.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.26.0,<2.0.0
opentelemetry-instrumentation>=0.47b0,<2.0.0

# Groq instrumentation (the one that was causing backtracking)
opentelemetry-instrumentation-groq==0.58.1

# Optional: Suppress excessive telemetry if you don't need it
# traceloop-sdk  (literalai pulls this, so we pin a stable version)
traceloop-sdk>=0.15.0,<1.0.0